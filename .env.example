# joshua421 — your own credentials (dogfood: just you, your own tokens)

# Google OAuth (your account — read Calendar / Gmail / Drive, write Calendar / email)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# Anthropic — the reflector that writes the reflections
ANTHROPIC_API_KEY=

# Where the cairn (behaviour-only) lives on this machine
CAIRN_DB_PATH=./cairn.sqlite
